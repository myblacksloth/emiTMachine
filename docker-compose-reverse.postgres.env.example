# Copy this file to docker-compose-reverse.postgres.env.
# Keep this value aligned with the password part of DATABASE_URL
# in docker-compose-reverse.backend.env.
POSTGRES_PASSWORD=change-this-password
