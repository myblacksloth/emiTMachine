# Copy this file to docker-compose-reverse.frontend.env.

# Comma-separated list of public hostnames allowed by the Vite dev server.
# Use only hostnames, without protocol and without path.
VITE_ALLOWED_HOSTS=your.domain.com
